Graphics Material Instance
==========================

The :code:`sol::GraphicsMaterialInstance` class inherits from :code:`sol::MaterialInstance`. Currently, it adds no
additional data or behaviour to its base class.
